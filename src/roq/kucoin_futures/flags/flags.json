{
  "name": "Flags",
  "type": "flags",
  "values": [
    {
      "name": "exchange",
      "type": "std::string",
      "required": true,
      "default": "kucoin-futures",
      "description": "exchange identifier (string)"
    },
    {
      "name": "api",
      "type": "std::string",
      "required": true,
      "default": "v1",
      "description": "what api to use (v1, v2)"
    }
  ]
}
